# 番茄工作法配置
# 每25分钟提醒一次，配合番茄工作法使用

data_dir: ~/daily_summary/data
summary_dir: ~/daily_summary/summaries

# 番茄工作法：每25分钟记录一次
minute_interval: 25

# 晚上11点生成今天的总结
summary_time: "23:00"

claude_code_path: claude-code

# 3分钟超时
dialog_timeout: 180

enable_logging: true
