# 测试配置 - 每5分钟提醒一次
# 用于快速测试程序功能

data_dir: ~/daily_summary_test/data
summary_dir: ~/daily_summary_test/summaries

# 使用分钟级调度：每5分钟提醒一次
minute_interval: 5

# 测试总结生成：每天23:59生成（避免凌晨）
summary_time: "23:59"

claude_code_path: claude-code

# 1分钟超时（测试用）
dialog_timeout: 60

enable_logging: true
